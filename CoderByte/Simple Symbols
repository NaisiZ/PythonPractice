def SimpleSymbols(str):
    str = " " + str + " " //prevent str[i-1] out of boundry
    for i in range(1, len(str)):
        if str[i].isalpha():
            if str[i - 1] != '+' or str[i + 1] != "+":
                return "false"
    return "true"
print(SimpleSymbols("+d+=3=+s+"))















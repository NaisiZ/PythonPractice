#PygLatin translater project from codecademy. This project meant to practice a combination of declaire variable, assign variable, raw_input function
if/else loop, flow, isalpha function, .lower function, string concatenate.

pyg = 'ay'

original = raw_input('Enter a word:')

if len(original) > 0 and original.isalpha():
    word = original.lower()
    first = word[0]
    new_word = word[1:] + first + pyg
    print new_word
else:
    print 'empty'
